// The repo-root logo.svg is the single source of truth (also used for the
// README and to generate the marketplace icon). Vite bundles it as an
// addressable, same-origin asset so it is safe to use under the extension CSP.
import logoUrl from '../../../logo.svg';

export { logoUrl };
